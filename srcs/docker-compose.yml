<<<<<<< HEAD
# **************************************************************************** #
#                                                                              #
#                                                         :::      ::::::::    #
#    docker-compose.yml                                 :+:      :+:    :+:    #
#                                                     +:+ +:+         +:+      #
#    By: malatini <marvin@42.fr>                    +#+  +:+       +#+         #
#                                                 +#+#+#+#+#+   +#+            #
#    Created: 2021/11/19 16:40:58 by malatini          #+#    #+#              #
#    Updated: 2021/11/19 16:40:59 by malatini         ###   ########.fr        #
#                                                                              #
# **************************************************************************** #

version: '3.6'
=======
version: '3'
>>>>>>> parent of 78bd9f6... save

volumes:
  mariadb-volume:
    driver: local
    driver_opts:
      device: /home/malatini/data/database
      type: volume
      o: bind
  wordpress-volume:
    driver: local
    driver_opts:
      device: /home/malatini/data/wordpress
      type: volume
      o: bind

networks:
  mariadb-network:
  nginx-network:

services:
  nginx:
    container_name: 42nginx
    build:
      context: ./requirements/nginx
    image: 42_nginx
    volumes:
      - wordpress-volume:/var/www/wordpress
    networks:
      - nginx-network
    ports:
      - "443:443"
    restart: unless-stopped

  mariadb:
    container_name: 42mariadb
    build:
      context: ./requirements/mariadb
    image: 42_mariadb
    volumes:
      - mariadb-volume:/var/lib/mysql
    networks:
      - mariadb-network
    environment:
      - MARIADB_ROOT_PWD=${MARIADB_ROOT_PWD}
      - MARIADB_DB=${MARIADB_DB}
      - MARIADB_USER=${MARIADB_USER}
      - MARIADB_PWD=${MARIADB_PWD}
    restart: unless-stopped

  wordpress:
    container_name: 42wordpress
    depends_on:
      - mariadb
    build:
      context: ./requirements/wordpress
    image: 42_wordpress
    volumes:
      - wordpress-volume:/var/www/wordpress
    networks:
      - nginx-network
      - mariadb-network
    environment: 
      - WP_URL=${WP_URL}
      - WP_DATABASE=${WP_DATABASE}
      - WP_TITLE=${WP_TITLE}
      - WP_ADMIN_USER=${WP_ADMIN_USER}
      - WP_ADMIN_PWD=${WP_ADMIN_PWD}
      - WP_ADMIN_EMAIL=${WP_ADMIN_EMAIL}
      - WP_USER=${WP_USER}
      - WP_USER_PWD=${WP_USER_PWD}
      - WP_USER_EMAIL=${WP_USER_EMAIL}
      - MARIADB_HOST=${MARIADB_HOST}
      - MARIADB_USER=${MARIADB_USER}
      - MARIADB_PWD=${MARIADB_PWD}
      - MARIADB_DB=${MARIADB_DB}
<<<<<<< HEAD
    restart: on-failure

volumes:
  mariadb-volume:
  # "driver specifies a logging driver for the service's container"
  # local means the volume are created on the same Docker host on which the containers are running.
  # we could create volumes on external hosts.
  # Par defaut sur linux les volumes sont montes sur /var/lib/docker/volumes/
    driver: local
    # Les trois options sont necessaires sinon le build ne fonctionne pas
    driver_opts:
    #   # Les volumes doivent se trouver dans ce dossier sur la machine hote.
    #   # Demande express du sujet
      device: /home/malatini/data/database
      type: volume
      o: bind
  wordpress-volume:
    driver: local
    driver_opts:
      device: /home/malatini/data/wordpress
      type: volume
      o: bind

# Definition de notre network (comme sur le schema d'architecture)
# Les containers seront reachable uniquement par les autres containers du meme reseau
# Par defaut docker cree un reseau meme si rien n'est specifie
# La directive doit etre mentionnee (selon le sujet).
networks:
  inception-network:
=======
    restart: unless-stopped
>>>>>>> parent of 78bd9f6... save
